import { prisma } from "../config/database.js";

/**
 * CatalogService
 * Business logic for leather catalog operations.
 * Implementation will be added in Step 2.
 */
export class CatalogService {
  // TODO: list(filters) — paginated listing with origin/color/finish/price filters
  // TODO: getById(id) — single leather with supplier info
  // TODO: create(data, supplierId) — create new leather listing
  // TODO: update(id, data, supplierId) — update listing (owner check)
  // TODO: remove(id, supplierId) — soft or hard delete (owner check)
}
