import { prisma } from "../config/database.js";

/**
 * OpenAIService
 * Handles communication with the OpenAI API for the AI virtual assistant.
 * The assistant suggests leather types based on user prompts.
 * Implementation will be added in Step 2.
 */
export class OpenAIService {
  // TODO: chat(sessionId, userMessage) — send message to OpenAI with context, store history
  // TODO: getHistory(sessionId) — retrieve chat message history
  // TODO: buildSystemPrompt() — construct the system prompt with leather knowledge
}
