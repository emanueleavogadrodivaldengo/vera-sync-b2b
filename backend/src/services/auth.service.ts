import { prisma } from "../config/database.js";

/**
 * AuthService
 * Business logic for user authentication.
 * Implementation will be added in Step 2.
 */
export class AuthService {
  // TODO: register(data) — hash password, create user in DB
  // TODO: login(email, password) — verify credentials, return JWT
  // TODO: getUserById(id) — fetch user profile
  // TODO: validateNextAuthToken(token) — verify NextAuth JWT for Express routes
}
