import { prisma } from "../config/database.js";

/**
 * CartService
 * Business logic for shopping cart and sample request creation.
 * Implementation will be added in Step 2.
 */
export class CartService {
  // TODO: getCart(userId) — return cart items with leather details
  // TODO: addItem(userId, leatherId, quantity, notes) — upsert cart item
  // TODO: updateItem(id, userId, data) — update quantity/notes
  // TODO: removeItem(id, userId) — delete cart item
  // TODO: checkout(userId, message) — create SampleRequest from cart, clear cart
}
