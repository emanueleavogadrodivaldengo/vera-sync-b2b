/**
 * Login Page — Authenticate users (buyers & suppliers).
 * Uses NextAuth (Auth.js) credentials provider.
 * Components will be built in Step 2.
 */
export default function LoginPage() {
  return (
    <div>
      <h1>Sign In</h1>
      {/* TODO: Login form (email + password) */}
      {/* TODO: Link to register page */}
    </div>
  );
}
